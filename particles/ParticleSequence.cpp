#include "ParticleSequence.h"

namespace Core {

    ParticleSequence::ParticleSequence(UInt32 start, UInt32 length) {
        this->start = start;
        this->length = length;
    }

    ParticleSequence::~ParticleSequence() {

    }

}