#include "GeometryUtils.h"

namespace Core {

    WeakPointer<Mesh> GeometryUtils::createGrid(Real width, Real height, UInt32 hSubDivisions, UInt32 vSubDivisions) {
        WeakPointer<Mesh> gridMesh;

        return gridMesh;
    }

}